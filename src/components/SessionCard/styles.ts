import { StyleSheet } from "react-native";

export default StyleSheet.create({
  wrapper: {
    marginHorizontal: 12,
    marginVertical: 12,
    justifyContent: "center",
    alignItems: "center",
  },
  cardContainer: {
    borderRadius: 12,
    borderWidth: 0,
    justifyContent: "center",
    alignItems: "center",
    width: "100%",
    minHeight: 200,
  },
});
